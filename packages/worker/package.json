{
  "name": "@bangumi-tv/worker",
  "version": "1.0.0",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test",
    "build:check": "wrangler deploy --dry-run --outdir dist"
  },
  "dependencies": {
    "@bangumi-tv/shared": "workspace:*",
    "hono": "^4.12.27"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "wrangler": "^4.x"
  }
}
